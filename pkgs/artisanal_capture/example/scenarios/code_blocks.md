# Code Blocks

Fenced code with a language label:

```dart
// Literal Markdown: **not bold** and [not a link](nope).
print('<b>literal HTML</b>');
```

Indented code follows:

    # still literal Markdown
    <section>still literal HTML</section>
    **literal stars**

END_CODE
