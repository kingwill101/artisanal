# Wrapping Stress

Long token:
`supercalifragilisticexpialidocious_without_breaks_0123456789ABCDEFGHIJKLMN`

Long URL: https://example.test/a/very/long/path/with/a/query?alpha=one&beta=two&tail=visible

Long link text: [A link whose readable label is intentionally much longer than
the available narrow terminal width](https://example.test/docs/long-label).

The sentence after every stress case proves that the renderer recovered.

END_WRAP
