name: artisanal_capture
description: Deterministic terminal frame capture and native image export for Artisanal.
version: 0.1.0
repository: https://github.com/kingwill101/artisanal
resolution: workspace

environment:
  sdk: ">=3.10.0 <4.0.0"

executables:
  artisanal_capture:

dependencies:
  artisanal: ^0.6.2
  artisanal_widgets: ^0.4.2
  image: ^4.7.1
  ultraviolet: ^0.5.1

dev_dependencies:
  crypto: ^3.0.7
  lints: ^6.0.0
  test: ^1.28.0
