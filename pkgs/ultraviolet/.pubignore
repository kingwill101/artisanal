# Assets are referenced in the README via GitHub raw URLs.
# Excluding them keeps the package lean on pub.dev and in cache.
assets/

# DevTools sessions are generated by the profiling tasks.
benchmark/profiles/
