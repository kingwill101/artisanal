/// Native software rasterization entry point for UV buffers.
///
/// This library intentionally is not exported by the browser-safe UV
/// entrypoints: it uses `pure_ui`'s TrueType parser and `package:image`.
library;

export 'src/raster/raster.dart';
