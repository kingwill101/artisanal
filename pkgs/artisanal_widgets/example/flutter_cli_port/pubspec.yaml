name: flutter_cli_port
description: Artisanal Widgets port of the Rust flutter-cli dashboard.
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.10.0 <4.0.0"

executables:
  flutter-cli: main
  flutter_cli_port: main

dependencies:
  artisanal: ^0.5.0
  artisanal_widgets: ^0.3.0

dev_dependencies:
  test: ^1.28.0
