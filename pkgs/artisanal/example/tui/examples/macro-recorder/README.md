# Macro Recorder/Player

```bash
dart run pkgs/artisanal/example/tui/examples/macro-recorder/main.dart
```

What to expect:
- records scripted `KeyMsg` traffic into a `ProgramMacro`,
- replays that macro through `Program` replay options,
- loops playback on a running program and stops it via `stopMacroPlayback()`.
