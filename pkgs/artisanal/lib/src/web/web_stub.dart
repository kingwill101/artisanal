/// Web/Flutter support stub for non-web (VM/CLI) contexts.
///
/// This library intentionally exports nothing that depends on `dart:js_interop`
/// so that the main barrel can conditionally re-export the real web surface
/// only when running on the web platform.
library;
