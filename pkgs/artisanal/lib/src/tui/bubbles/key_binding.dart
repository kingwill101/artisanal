/// Key binding primitives, now part of the core TUI runtime.
///
/// Re-exported here for backward compatibility with code that imported
/// `package:artisanal/bubbles.dart`'s key binding helpers.
library;

export '../key_binding.dart';
