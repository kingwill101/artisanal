/// Display-only UI building blocks.
///
/// These components follow bubble conventions by rendering to strings, allowing
/// them to be used directly (print) or composed into a model's `view()`.
library;

export 'components/components.dart';
