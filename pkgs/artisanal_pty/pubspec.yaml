name: artisanal_pty
description: An embeddable ANSI terminal emulator and pty2 widget for Artisanal.
version: 0.1.1
repository: https://github.com/kingwill101/artisanal
resolution: workspace

environment:
  sdk: ">=3.10.0 <4.0.0"

platforms:
  linux:
  macos:
  windows:
  web:

dependencies:
  artisanal: ^0.6.0
  artisanal_widgets: ^0.4.0
  characters: ^1.4.0
  pty2: ^0.5.4

dev_dependencies:
  lints: ^6.0.0
  test: ^1.28.0
