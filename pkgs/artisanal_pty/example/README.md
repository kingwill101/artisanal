# Artisanal PTY example

Run the example from the repository root:

```sh
dart run pkgs/artisanal_pty/example/main.dart
```

The example opens the current user's interactive shell inside an
`artisanal_widgets` application. Exit the shell or press the application's
quit key to return to the host terminal.
