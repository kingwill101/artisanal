# Changelog

## Unreleased

- Move the GitHub dashboard from `pkgs/artisanal_widgets/example/github_cli`
  to `apps/github_cli` as a standalone workspace application.
- Update replay scenario lookup for the new application location.
- Add the Dart SDK SIMD issue as an offline app-level Markdown/F12 regression
  bench. Commented-out disclosures stay hidden, and enabling the debug overlay
  over linked tables no longer exposes OSC 8 payload text.
