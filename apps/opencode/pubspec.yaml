name: opencode
description: OpenCode-style terminal UI application built with Artisanal.
publish_to: none
resolution: workspace

executables:
  opencode:

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  artisanal: ^0.6.0
  artisanal_widgets: ^0.4.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
